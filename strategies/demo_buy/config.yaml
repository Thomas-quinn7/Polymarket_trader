# generated: pmf-7e3f-tq343
strategy: demo_buy

# ---------------------------------------------------------------------------
# Hold period
# ---------------------------------------------------------------------------
# How long (seconds) to hold each demo position before the strategy
# signals an exit.  Kept short so the full open → monitor → close cycle
# is observable in a single session.
hold_seconds: 60

# ---------------------------------------------------------------------------
# Market scope
# ---------------------------------------------------------------------------
# Polymarket categories this strategy will scan.
# Demo only needs a narrow scope to limit noise; crypto is the busiest.
scan_categories:
  - crypto
